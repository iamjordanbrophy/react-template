import React from 'react'
import style from './Header.css'

const Header = () =>
  <div className={style.header}>
    <h1>Header</h1>
    <p>The Header component above is a pure JavaScript <code>function</code> written in es6. A functional stateless component. It's a really fast and simple way to develope components that is recommended to be used wherever possible.</p>
  </div>

export default Header